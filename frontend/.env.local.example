# Optional: enables WalletConnect-based wallets in RainbowKit. Browser
# wallets (MetaMask, Rabby, etc.) work without this. Get a free project ID
# at https://cloud.reown.com.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
